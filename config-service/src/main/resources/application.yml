spring:
  application:
    name:
      config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mohammedalics/store-config.git
          basedir: ${user.home}/store-config
          clone-on-start: true
          force-pull: true
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
server:
  port: 8888

